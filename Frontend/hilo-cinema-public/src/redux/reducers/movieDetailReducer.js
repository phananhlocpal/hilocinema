const initialState = {

}

const movieDetailReducer = (state = initialState, action) => {
    switch (action.type) {
      case "...": {
        return {
            ...state,
            
          };
      }
      default:
        return state;
    }
}

export default movieDetailReducer;