const initialState = {

}

const detailPostReducer = (state = initialState, action) => {
    switch (action.type) {
      case "...": {
        return {
            ...state,
            
          };
      }
      default:
        return state;
    }
}

export default detailPostReducer;